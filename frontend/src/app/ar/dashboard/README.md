# Arabic dashboard route

Prompt A should implement the Arabic-only dashboard under this route segment.

Expected role-specific entry points:

- `/ar/dashboard/member`
- `/ar/dashboard/coach`
- `/ar/dashboard/admin`

The dashboard must remain `noindex`, authenticated, role-guarded, mobile-first, and RTL-first.
