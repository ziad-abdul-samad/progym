# Prisma data model

`schema.prisma` is the Stage 1 database contract for Pro Gym.

No migration has been generated in this stage. Prompt A should turn this contract into reviewed migrations after authentication, memberships, QR attendance, progress tracking, and analytics behavior are implemented.
